start:
	echo "run make build"

#this is to build the project, and creating the zip file, if there is a name change to the project, fix the directory path in the build.sh file
build:
	sh ./scripts/build.sh
